using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodesButton : MonoBehaviour
{
/*
    PlayerManager player;

    public Color[] color;
    float mobi;
    MeshRenderer render;
    float distanceToPlayer;
    Vector3 _heading;

    private void Start()
    {
        player = FindObjectOfType<PlayerManager>();

        var heading = transform.position - player.OnActiveUnit1.gameObject.transform.position;
        _heading = heading;
        distanceToPlayer = heading.magnitude;
        render = GetComponentInChildren<MeshRenderer>();

        mobi = player.OnActiveUnit1.Mobility * 2.5f;


        for (int i = 1; i < 7; i++)
        {
            if (distanceToPlayer < ((mobi) *i))
            {
                render.material.color = color[i-1];
                break;
            }

        }
    }*/
}
